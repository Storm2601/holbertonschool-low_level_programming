0 : fonction qui convertit un nombre binaire en un nombre int non signé, où b pointe vers une chaîne de 0 et 1 caractères, retourne le nombre converti, ou 0 si
il y a un ou plusieurs caractères dans la chaîne b qui n'est pas 0 ou 1 b est NULL.

1 : fonction qui imprime la représentation binaire d'un nombre, pas le droit d'utiliser des tableaux ni malloc et pas autorisé à utiliser les opérateurs % ou /.

2 : fonction qui renvoie la valeur d'un bit à un indice donné, où index est l'indice, à partir de 0, du bit que vous voulez obtenir, retourne la valeur du bit à l'indice index ou -1 si une erreur s'est produite.

3 : fonction qui fixe la valeur d'un bit à 1 à un indice donné, où index est l'indice, à partir de 0, du bit que vous voulez mettre à 1, retourne 1 si cela a fonctionné, ou -1 si une erreur s'est produite.

4 : fonction qui met la valeur d'un bit à 0 à un index donné, où index est l'index, à partir de 0, du bit que vous voulez mettre à zéro, renvoie 1 si cela a fonctionné, ou -1 si une erreur s'est produite.

5 : fonction qui renvoie le nombre de bits à retourner pour passer d'un nombre à un autre, pas autorisé à utiliser les opérateurs % ou /.