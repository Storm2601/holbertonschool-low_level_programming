0 : fonction qui lit un fichier texte et l'imprime sur la sortie standard POSIX, où lettres est le nombre de lettres à lire et à imprimer renvoie le nombre réel de lettres qu'il a pu lire et imprimer, si le fichier ne peut être ouvert ou lu, renvoie 0, si le nom de fichier est NULL, retournez 0, si l'écriture échoue ou n'écrit pas le nombre d'octets prévu, retournez 0.

1 : fonction qui crée un fichier, où nom_de_fichier est le nom du fichier à créer et texte_contenu est une chaîne de caractères terminée par NULL à écrire dans le fichier, Retourne : 1 en cas de succès, -1 en cas d'échec (le fichier ne peut pas être créé, le fichier ne peut pas être écrit, l'écriture "échoue", etc...). Le fichier créé doit avoir les permissions suivantes : rw-------. Si le fichier existe déjà, ne changez pas les permissions,  si le fichier existe déjà, le tronquer, si le nom du fichier est NULL, retourner -1, si text_content est NULL créer un fichier vide.

2 : fonction qui ajoute du texte à la fin d'un fichier, ù nom_du_fichier est le nom du fichier et contenu_du_texte est la chaîne de caractères à terminaison NULL à ajouter à la fin du fichier. Retourne : 1 en cas de succès et -1 en cas d'échec, Ne pas créer le fichier s'il n'existe pas, Si le nom du fichier est NULL, renvoyer -1, Si text_content est NULL, ne rien ajouter au fichier. Retourne 1 si le fichier existe et -1 s'il n'existe pas ou si vous n'avez pas les permissions nécessaires pour écrire le fichier.

3 : programme qui copie le contenu d'un fichier dans un autre fichier, si le nombre d'arguments n'est pas correct, quitter avec le code 97 et imprimer Usage : cp fichier_de_fichier_à, suivi d'une nouvelle ligne, sur l'erreur standard POSIX, si le fichier_to existe déjà, le tronquer
    si le fichier_from n'existe pas, ou si vous ne pouvez pas le lire, quittez avec le code 98 et imprimez Erreur : Can't read from file NAME_OF_THE_FILE, suivi d'une nouvelle ligne, sur l'erreur standard POSIX, où NOM_DU_FICHIER est le premier argument passé à votre programme
    si vous ne pouvez pas créer de fichier ou si l'écriture dans le fichier_to échoue, quittez le programme avec le code 99 et affichez Error : Can't write to NAME_OF_THE_FILE, suivi d'une nouvelle ligne, sur l'erreur standard POSIX, où NOM_DU_FICHIER est le deuxième argument transmis à votre programme, si vous ne pouvez pas fermer un descripteur de fichier, quittez avec le code 100 et affichez Error : Can't close fd FD_VALUE, suivi d'une nouvelle ligne, sur l'erreur standard POSIX,  où FD_VALEUR est la valeur du descripteur de fichier, Permissions du fichier créé : rw-rw-r--. Si le fichier existe déjà, ne modifiez pas les permissions, Vous devez lire 1 024 octets à la fois dans le fichier_from pour effectuer moins d'appels système. Utiliser un tampon. Vous êtes autorisé à utiliser dprintf.