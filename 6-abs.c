#include "main.h"

/**
 * int _abs(int) - 'int' function absolute value of an integer
 * _abs give the function of integer 
 * Return: n with int _abs(int) Returns valeurs of integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = n * (-1);
	}
	return (n);
}
