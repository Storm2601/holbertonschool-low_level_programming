Script 0: Écrivez un script qui exécute un fichier C via le préprocesseur et enregistrez le résultat dans un autre fichier.
       Le nom du fichier C sera enregistré dans la variable $CFILE. La sortie doit être enregistrée dans le fichier c.

Script 1 : compile un fichier C mais ne crée pas de lien,Le nom du fichier C sera enregistré dans la variable $CFILE,Le fichier de sortie doit porter le même nom que le fichier C, mais avec l'extension .o au lieu de .c.

Script 2 : génère le code assembleur d'un code C et enregistrez-le dans un fichier de sortie,Le nom du fichier C sera enregistré dans la variable $CFILE Le fichier de sortie doit porter le même nom que le fichier C, mais avec l'extensio	n .s au lieu de .c.

Script 3 : compile un fichier C et crée un exécutable nommé cisfun,Le nom du fichier C sera enregistré dans la variable $CFILE

Script 4 : programme C qui affiche exactement "La programmation, c'est comme construire un puzzle multilingue, en utilisant la fonction puts, suivi d'une nouvelle ligne

Script 5 : programme C qui s'imprime exactement avec une grammaire appropriée, mais le résultat est une œuvre d'art, suivie d'une nouvelle ligne, en utilisant la fonction printf, Votre programme devrait renvoyer 0
       Votre programme doit se compiler sans avertissement lors de l'utilisation de l'option -Wall gcc

Script 6 : programme C qui imprime la taille de différents types sur l'ordinateur sur lequel il est compilé et exécuté, et doit renvoyer 0
