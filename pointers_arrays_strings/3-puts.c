#include <stdio.h>
#include "main.h"

/**
 * _puts - imprime une chaîne de caractères, suivie d'une nouvelle ligne
 * @str: strings
 * Return: void
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
