#include "main.h"

/**
 * _islower - Check if c is lowercase or not
 * @c: character
 * Return: if c is lowercase - Returns 0 otherwise
 */

int _islower(int c) /*init _islowercase if 1 or 0 for 'C' */
{
	if (c >= 'a' && c <= 'z')

	return (1);

	return (0);
}
