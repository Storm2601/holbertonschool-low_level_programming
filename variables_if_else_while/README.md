Script 0 : attribuera un nombre aléatoire à la variable n à chaque fois qu’elle sera exécutée et affiche si le nombre stocké dans la variable n est positif ou négatif et stockera une variable différente à chaque fois que nous éxécuterons le programme. Le résultat du programme doit être si le nombre est supérieur à 0: c'est positif, si le nombre est 0, 0 égal à zéro, si le nombre est inférieur à 0, 0 est négatif.

Script 1 : atribuera un nombre aléatoire à la variable n à chaque fois qu'elle sera éxécutée. en complétent le code source afin d'afficher le dernier chiffre stocké dans la variable et stockera une variable différente.

Script 2 : imprime l'alphabet en minuscules, suivi d'une nouvelle ligne en utilisant que putchar.

Script 3 : imprime l'alphabet en minuscules, puis en majuscules, suivi d'une nouvelle ligne en utilisant que putchar.

Script 4 : imprime l'alphabet en minuscules, suivi d'une nouvelle ligne en utilisant que putchar.

Script 5 : imprime tous les nombres à un chiffre de la base 10 à partir de 0, suivi d'une nouvelle ligne et doit se trouver dans la fonction principale.

Script 6 : imprime tous les nombres à un chiffre de la base 10 à partir de 0, suivi d'une nouvelle ligne en utilisant putchar 2 fois.

Script 7 : imprime l'alphabet minuscule à l'envers, suivi d'une nouvelle ligneet doit se trouver dans la fonction principale en utilisant putchar 2 fois.

Script 8 : imprime tous les nombres de base 16 en minuscules, suivis d'une nouvelle ligne et doit se trouver dans la fonction principale en utilisant putchar 2 fois.

Script 9 : imprime toutes les combinaisons possibles de nombres à un chiffre, Les nombres doivent être séparés par , , suivi d'un espace dans l'ordre croissant dans la fonction principale en utilisant putchar 2 fois sans utiliser de variable de type char.